# M0.6 Adapter Conformance Tooling — Exit Candidate

Status: **PENDING VALIDATOR**

Issue: #87  
Parent: #81

## Purpose

This document binds the Phase 5 implementation candidate for the M0.6 milestone exit audit. It records the evidence the Validator must review; it does not issue the Validator verdict and it does not authorize the final Phase merge or milestone closure.

## Reviewed milestone scope

M0.6 provides reusable Core-side tooling for external adapter projects to demonstrate conformance with published Adapter Protocol 0.1 / Public Contract 0.1 behavior while preserving these boundaries:

- conformance is interoperability evidence, not solver-native physical/numerical validation;
- conformance results are distinct from SOL lifecycle/evaluation classification;
- adapter implementations are observed through the published transport boundary rather than linked into Core;
- ProtocolFailure, valid negative responses, transport/harness failure, and lifecycle state remain distinct;
- execute response loss grants no automatic replay authority;
- backend-native identifiers remain opaque provenance rather than canonical semantic identity;
- exact conformance CLI spelling, exit codes, and stable serialized report output remain provisional unless separately approved as a public interface.

## Phase evidence entering the audit

| Phase | Issue | Evidence entering Phase 5 |
| --- | --- | --- |
| 0 — result boundary | #82 | Conformance-specific result algebra; harness failure separation; physical-correctness exclusion; provisional CLI/output boundary |
| 1 — external harness | #83 | Reusable external-process invocation over the published transport |
| 2 — positive fixtures | #84 | Published Protocol/Public Contract positive fixtures executed through the harness |
| 3 — adversarial matrix | #85 | Valid-negative, ProtocolFailure, scheduling/effect/provenance, transport-loss and replay boundaries exercised |
| 4 — authoring skeleton | #86 | Solver-independent external-style adapter skeleton and guide; positive subprocess conformance evidence |
| 5 — external workflow | #87 | Standalone consumer workspace, temporary published fixture copy, positive/adversarial execution, deterministic provisional machine-readable CI observation |

The immutable exact-head Phase 5 CI evidence is recorded on PR #108 / issue #87 after the implementation head becomes green. This document intentionally does not embed a self-referential candidate commit hash.

## Phase 5 external-project evidence

`examples/external-project-conformance/` is a nested standalone Cargo workspace. Its manifest depends on conformance libraries only and does not link the reference adapter implementation, MockAdapter implementation, or a solver runtime.

Rust Core CI supplies executable paths for the reference adapter and the adversarial probes. The consumer invokes them using `ExternalAdapterCommand`, so adapter behavior crosses the JSON-RPC/stdin-stdout process boundary before becoming conformance evidence.

The fixture directory is copied into the CI runner temporary directory before the standalone workflow runs. The consumer therefore demonstrates runtime reuse of the published versioned fixture material through an explicit path rather than a Core-relative fixture assumption.

## Expected executable observations

The Phase 5 workflow is acceptable only when all four observations remain deterministic:

| Observation | Expected determination | Meaning |
| --- | --- | --- |
| positive | `Conformant` | External reference adapter matches the published positive baseline |
| adversarial valid-negative | `Conformant` | Published negative/failure semantics are represented correctly |
| semantic violation detection | `NonConformant` | Parsable dependency/effect/provenance violations are rejected as adapter non-conformance |
| execute response loss | `NotEstablished` | Ambiguous transport loss remains harness/transport evidence and does not authorize replay |

The workflow emits a milestone-local JSON snapshot twice and requires byte-for-byte equality. The snapshot is explicitly labeled `provisional_not_a_public_cli_or_report_contract`.

## Milestone gate audit requested from Validator

Validator should challenge at least the following gates:

| Gate | Candidate evidence | Required finding |
| --- | --- | --- |
| External invocation | Standalone consumer receives executable paths and uses the external harness | No adapter/backend implementation linkage into Core conformance tooling |
| Fixture reuse | Published 0.1 fixture directory copied to a temporary external path | No duplicated/reinterpreted Protocol/Public Contract semantics |
| Positive behavior | `PublishedPositiveFixtureSuite` against the Phase 4 authoring skeleton | `Conformant` |
| Valid-negative behavior | Full reference adversarial valid-negative matrix | `Conformant` |
| Semantic counterexamples | Dependency schedule, aggregate effect, provenance identity probes | `NonConformant` |
| Transport separation | Execute response-loss probe | `NotEstablished`; no replay authority |
| Result/lifecycle separation | Phase 0 conformance result model and Phase 5 snapshot | No SOL PASS/FAIL/BLOCKED/INDETERMINATE reinterpretation |
| Backend validation separation | Snapshot and guides state `not_assessed_by_conformance` | No physical/numerical correctness claim |
| Solver independence | Consumer and conformance manifests contain no solver dependency | PASS |
| Public-interface boundary | Snapshot and guides mark exact output/CLI provisional | No accidental stabilized CLI/report schema |
| Published contract preservation | Existing Protocol/Public Contract schema gates remain in exact-head CI | No Protocol 0.1/Public Contract 0.1 meaning change |
| Real-adapter roadmap boundary | Guides keep first real solver adapter outside Core | PASS |

## Rejected implications

The exit candidate rejects these implications:

```text
conformance consumer -> linked adapter implementation
copied published fixture -> duplicated semantic contract
expected ProtocolFailure -> adapter non-conformance
transport response loss -> ProtocolFailure
transport response loss -> automatic execute replay
opaque backend reference -> canonical semantic identity
conformance determination -> SOL lifecycle classification
conformance determination -> backend physical/numerical correctness
provisional CI JSON -> stable public report schema
reference environment variable/profile name -> stable public CLI
M0.6 completion -> real solver adapter embedded in Core
```

## Closure authority

This file must remain `PENDING VALIDATOR` until an explicit Validator verdict is anchored to the exact final Phase 5 candidate and required CI is green on that same head.

Operator must not use bounded auto-merge for the Phase 5 exit PR. After Validator approval and any required milestone-exit authorization, the final Phase may be merged, verified on `main`, and then #87, parent #81, and GitHub Milestone #6 may be closed in the accepted order.
