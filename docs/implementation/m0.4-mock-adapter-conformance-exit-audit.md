# M0.4 MockAdapter Protocol Conformance Exit Audit

**Status:** COMPLETE — Validator APPROVE and closure gates satisfied  
**Date:** 2026-08-21  
**Milestone:** M0.4 — MockAdapter Protocol Conformance  
**Parent tracker:** #65  
**Exit gate:** #71

## Purpose

This audit evaluates whether M0.4 has made MockAdapter an executable in-process reference implementation of the published transport-independent Adapter Protocol 0.1 baseline without changing published protocol meaning, introducing transport behavior, or claiming solver-native physical correctness.

The audit is an acceptance review of the M0.4 implementation boundary. It does not publish a new Adapter Protocol version and does not make MockAdapter a real solver adapter.

## Validator verdict

**APPROVE.**

The Phase 0–5 implementation evidence is internally consistent with the published Adapter Protocol 0.1/Public Contract 0.1 boundary. No unresolved semantic, architecture, Public Contract, Adapter Protocol, roadmap, or compatibility contradiction was identified in this audit.

The original mechanical and repository closure gates are satisfied. Phase 5 exact head `311b9ed1a012ca498024c681fff658a488d38a01` passed Rust Core CI #542 (`32531121694`), PR #93 merged as `2cd12aa9f8de76a761e96ac791f9e91a81787ff2`, the merge was verified on `main`, #71 and parent #65 closed as completed, parent #65 was removed from Milestone #1 membership, and GitHub Milestone #1 closed.

## Gate audit

| Gate | Result | Evidence / interpretation |
|---|---|---|
| Published DTO authority | PASS | Protocol-facing MockAdapter methods consume/return `sol-adapter-protocol` and `sol-public-contract` DTOs rather than defining duplicate canonical semantic payloads. |
| Adapter description / dual compatibility | PASS | Phase 1 provides deterministic `adapter.mock` description, target/capability evidence, and independent Protocol/Public Contract compatibility. |
| Advisory preflight | PASS | Phase 2 implements deterministic `validate_plan`, target/capability/action/precondition diagnostics, transient availability, and equivalent-request/state stability without durable authorization. |
| Authoritative execution | PASS | Phase 3 rechecks current execution-critical state and does not trust prior preflight as execution authority. |
| MappingPlan dependency semantics | PASS | Execution reports preserve dependencies while alternate valid ordering and parallel independent batches remain permitted. |
| Realization reporting | PASS | Exact/degraded/unsupported/partial/unavailable evidence uses published execution/effect DTOs; aggregate effects are validated against action-level effects. |
| Failure algebra | PASS | Phase 4 uses published `ProtocolFailure` compatibility/invalid-request/operational categories and preserves `side_effects` evidence. |
| Valid negative response separation | PASS | Rejected/unavailable preflight and rejected/partial/unavailable execution remain normal operation responses rather than being collapsed into `ProtocolFailure`. |
| Validate idempotency boundary | PASS | Equivalent canonical validation request plus equivalent relevant state yields stable normalized output. |
| Execute replay boundary | PASS | Execute remains non-idempotent by default; repeated attempts recheck current state and no hidden deduplication/no-op success is introduced. |
| Ambiguous response-loss safety | PASS | Missing execute result remains conservative `may_have_occurred` evidence and does not become automatic replay authority. |
| Lifecycle separation | PASS | Protocol-facing outputs do not produce SOL `PASS`/`FAIL`/`BLOCKED`/`INDETERMINATE` lifecycle classification. |
| Provenance boundary | PASS | Opaque namespaced references are accepted only as evidence; backend-native or opaque-reference semantic-identity dependence is rejected. |
| Transport exclusion | PASS | M0.4 requires no JSON-RPC, stdio framing, request IDs, subprocess/session lifecycle, network transport, or reconnect/backoff behavior. |
| Solver dependency exclusion | PASS | MockAdapter conformance remains solver-installation independent and introduces no MOOSE/COMSOL/Ansys runtime dependency. |
| Physical correctness claim | PASS | Protocol conformance is explicitly not solver-native physical/numerical correctness evidence. |
| External conformance tooling scope | PASS | M0.4 remains an in-process Core reference suite; reusable external-adapter runner/tooling stays assigned to M0.6. |

## Executable conformance matrix

The final M0.4 suite includes dedicated tests for:

- description and dual compatibility;
- accepted and adversarial preflight;
- exact/degraded/unsupported/partial/unavailable execution;
- changed/current-state authoritative rejection;
- dependency-safe alternate and parallel schedules;
- typed ProtocolFailure and side-effect evidence;
- repeated validation and repeated execution state handling;
- ambiguous response-loss safety;
- lifecycle separation;
- opaque provenance and backend-native leakage counterexamples;
- source/dependency guards preventing transport and solver coupling;
- an integrated Phase 5 positive/adversarial operation matrix.

Published M0.3 fixtures are reused as executable evidence where applicable rather than copied into MockAdapter-specific semantic representations.

## Counterexamples that remain rejected

M0.4 does not permit any of the following interpretations:

```text
accepted validate_plan -> durable execute authorization
same execute request -> same execution attempt
no execute response -> no side effect
no execute response -> safe automatic replay
ProtocolFailure -> SOL lifecycle state
execution failed/rejected/unavailable -> ProtocolFailure by definition
canonical plan representation order -> mandatory physical total order
opaque backend reference -> canonical semantic identity
protocol conformance -> backend physical correctness
MockAdapter conformance -> real solver validation
```

## Accepted deferrals

- JSON-RPC / process stdio transport, framing, request IDs, process/session lifecycle, transport errors, reconnect/backoff, and transport replay orchestration remain M0.5.
- Reusable external-adapter conformance runner/tooling remains M0.6.
- Real solver adapters remain separate repositories/projects after reusable Core-side foundations.
- Stable SDK ergonomics remain downstream of the published contract/protocol boundaries.

## Final merge / closure evidence

All M0.4 closure conditions are satisfied:

1. Phase 5 exact head `311b9ed1a012ca498024c681fff658a488d38a01` passed the complete required CI sequence in Rust Core CI #542 (`32531121694`);
2. no unresolved review, merge conflict, permission, or compatibility gate remained;
3. the user-controlled milestone-exit PR #93 merged as `2cd12aa9f8de76a761e96ac791f9e91a81787ff2`;
4. merged `main` was verified;
5. #71 and parent #65 closed as completed, #65 was removed from Milestone #1 membership, and Milestone #1 closed with Phase issues #66–#71 as its canonical progress units;
6. this post-closure Operator `update` synchronizes repository documentation before M0.5 implementation begins.

M0.4 is complete. M0.5 may begin from a repository state that includes this completion synchronization.
