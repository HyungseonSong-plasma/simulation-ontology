# Implementation Notes

Implementation-stage notes live here. They complement, but do not replace, the baseline plan in `docs/plans/` or executable evidence in CI.

- `phase1-data-model.md` — Core Ontology v0.1 data model and serialization boundary.
