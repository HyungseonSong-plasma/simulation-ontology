# Schema

Validation schemas for machine-readable ontology definitions will live here.

Planned work includes schema validation for entities, semantic relations, constraints, identifiers, and backend mappings.
