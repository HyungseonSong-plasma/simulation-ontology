# Thermal Reference Fixtures

This directory is reserved for the first end-to-end vertical slice defined by the Core Simulation Ontology v0.1 implementation plan.

Phase 1 will add the canonical Thermal reference model and serialization round-trip fixtures here. Until M0.1 is stable, these fixtures target the Mock Adapter only and must not encode MOOSE, COMSOL, or Ansys native objects.
