[package]
name = "sol-adapter-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

description = "Transport-independent Adapter Protocol support for SOL"

[dependencies]
serde = { version = "=1.0.210", features = ["derive"] }
serde_json = "=1.0.128"
sol-public-contract = { path = "../sol-public-contract" }
